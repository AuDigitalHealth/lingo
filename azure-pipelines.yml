name: snomio-$(Date:yyyyMMdd)$(Rev:.r)_$(Build.SourceBranchName)

trigger:
  branches:
    include:
      - main
  tags:
    include:
      - '*'

variables:
  mavenCache: $(Pipeline.Workspace)/.m2/repository
  mavenOptions: '-Dmaven.repo.local=$(mavenCache) -Dmaven.settings.security=never'
  isRelease:  $[eq(startsWith(variables['Build.SourceBranch'], 'refs/tags/'), true)]
  feedName: 'snomio'

resources:
  repositories:
    - repository: sergio
      type: github
      name: aehrc/sergio
      endpoint: aehrc
      ref: rabbit-snomio-image-as-cli-entity-refactor

pool:
  vmImage: ubuntu-latest

jobs:
  - job: secrets_scan
    displayName: "Secret scan (TruffleHog + Gitleaks)"
    pool:
      vmImage: ubuntu-latest
    steps:
      - checkout: self
        fetchDepth: 0   # full history for scanners

      # TruffleHog (full history) — fails on findings
      - task: UsePythonVersion@0
        displayName: "Use Python (for TruffleHog)"
        inputs:
          versionSpec: "3.x"

      - script: |
          set -euo pipefail
          TH_VERSION="3.78.0"
          echo "Installing TruffleHog v${TH_VERSION}…"
          curl -sSL "https://github.com/trufflesecurity/trufflehog/releases/download/v${TH_VERSION}/trufflehog_${TH_VERSION}_linux_amd64.tar.gz" \
            | tar -xz trufflehog
          sudo mv trufflehog /usr/local/bin/trufflehog
          trufflehog --version
          
          echo "Running TruffleHog (full repo history)…"
          # Exit codes: 0 = clean, 183 = findings, other != error
          status=0
          trufflehog git file://. --json --no-update > trufflehog-report.json || status=$?
          
          if [ "$status" -eq 183 ]; then
            echo "❌ TruffleHog found potential secrets."
            exit 1
          elif [ "$status" -ne 0 ]; then
            echo "❌ TruffleHog error (exit $status)."
            exit "$status"
          else
            echo "✅ TruffleHog: no findings."
          fi
        displayName: "Run TruffleHog (full history)"

      # Gitleaks (all refs) — fails on findings
      - script: |
          set -euo pipefail
          GL_VERSION="8.18.2"
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${GL_VERSION}/gitleaks_${GL_VERSION}_linux_x64.tar.gz" \
            | tar -xz gitleaks
          sudo mv gitleaks /usr/local/bin/gitleaks
          # use your repo config if present
          CFG_FLAG=""
          [ -f ".gitleaks.toml" ] && CFG_FLAG="--config .gitleaks.toml"
          gitleaks detect --source . --log-opts="--all" ${CFG_FLAG} \
            --redact --report-format json --report-path gitleaks-report.json --exit-code 1
        displayName: "Run Gitleaks (all refs)"

      # Publish reports regardless of pass/fail
      - task: CopyFiles@2
        displayName: "Collect secret scan reports"
        condition: always()
        inputs:
          contents: |
            trufflehog-report.json
            gitleaks-report.json
          targetFolder: "$(Build.ArtifactStagingDirectory)/secret-scan"

      - task: PublishBuildArtifacts@1
        displayName: "Publish Secret Scan Reports"
        condition: always()
        inputs:
          PathtoPublish: "$(Build.ArtifactStagingDirectory)/secret-scan"
          ArtifactName: "secret-scan-reports"
          publishLocation: "Container"

  - job: trivy_scan
    displayName: "Dependency scan (Trivy)"
    pool:
      vmImage: ubuntu-latest
    steps:
      - checkout: self

      # Required so Maven can resolve internal SNAPSHOT artifacts from Azure Artifacts
      # without this, dependency:go-offline fails early and leaves ~/.m2 incomplete.
      - task: MavenAuthenticate@0
        displayName: 'Authenticate Maven (Azure Artifacts)'
        inputs:
          artifactsFeeds: '$(feedName)'

      # Use $(Agent.HomeDirectory) — tilde (~) is not expanded in Cache@2 path inputs
      # and would resolve literally to <sources>/~/.m2/repository.
      - script: mkdir -p $(Agent.HomeDirectory)/.m2/repository
        displayName: 'Create ~/.m2 directory'

      - task: Cache@2
        displayName: 'Cache Maven local repo (Trivy)'
        inputs:
          key: 'trivy-maven | "$(Agent.OS)" | **/pom.xml'
          restoreKeys: |
            trivy-maven | "$(Agent.OS)"
            trivy-maven
          path: $(Agent.HomeDirectory)/.m2/repository
          cacheHitVar: TRIVY_M2_HIT

      # Pre-populate the local Maven repository so Trivy can resolve POM parent chains
      # from disk rather than Maven Central. Skipped on an exact cache hit (the parent
      # POMs are already present); still runs on a miss/partial to fill gaps. Failures
      # are non-fatal because --offline-scan (below) prevents Trivy from hitting Maven
      # Central regardless.
      - script: |
          set -euo pipefail
          echo "##[section]Pre-populating Maven local repo for Trivy"
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          mvn dependency:go-offline \
            -Denforcer.skip=true \
            -DskipTests \
            -Dims-username=ci \
            -Dims-password=ci \
            -Dmaven.repo.local=$(Agent.HomeDirectory)/.m2/repository \
            --no-transfer-progress 2>&1 | tail -20 || true
        displayName: 'Pre-populate ~/.m2 for Trivy Maven scanning'
        condition: ne(variables.TRIVY_M2_HIT, 'true')

      - script: |
          set -euo pipefail
          echo "##[section]Installing Trivy (latest)"
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin
          trivy --version

          echo ""
          echo "##[section]Scan targets"
          echo "pnpm package files:"
          find . -name "package.json" -not -path "*/node_modules/*" -maxdepth 3 | sed 's/^/  /'
          echo "Maven POMs:"
          find . -name "pom.xml" -not -path "*/target/*" | sed 's/^/  /'

          # Download the vulnerability DB explicitly before running in offline mode.
          # --offline-scan skips all remote calls (including DB updates), so the DB
          # must be fetched here first to ensure the scan uses up-to-date data.
          echo ""
          echo "##[section]Downloading Trivy vulnerability databases"
          trivy fs --download-db-only .

          # Scan ONCE to JSON (the canonical report), then derive the table and SARIF
          # outputs from that JSON with `trivy convert` — no second/third filesystem
          # walk. We deliberately omit --exit-code here so the report is always written
          # (incl. for the always() publish + SARIF); the HIGH/CRITICAL gate is applied
          # from the JSON below.
          #
          # --offline-scan prevents Trivy from hitting Maven Central for parent POM
          # resolution (was causing FATAL 429 rate-limit errors); any POM not found in
          # ~/.m2 produces a WARN instead of aborting. ui/pom.xml is skipped (build
          # plugins only, and was causing a separate context-cancel crash).
          echo ""
          echo "##[section]Running Trivy ONCE (HIGH/CRITICAL, fixed only) -> JSON"
          trivy fs . \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --offline-scan \
            --skip-dirs node_modules \
            --skip-files ui/pom.xml \
            --format json \
            --output trivy-report.json \
            --scanners vuln

          echo ""
          echo "##[section]Converting JSON report to table + SARIF (no re-scan)"
          trivy convert --format table trivy-report.json
          trivy convert --format sarif --output trivy-report.sarif trivy-report.json

          # Gate from the JSON (preserves the previous fail-on-HIGH/CRITICAL behaviour).
          CRITICAL=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-report.json)
          HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-report.json)
          # Fail closed if the report did not yield numeric counts (e.g. empty/truncated JSON),
          # so an unparseable report can never fall through to a false PASS.
          if ! [[ "${CRITICAL}" =~ ^[0-9]+$ && "${HIGH}" =~ ^[0-9]+$ ]]; then
            echo "##[error]Trivy report did not yield numeric HIGH/CRITICAL counts (CRITICAL='${CRITICAL}' HIGH='${HIGH}') — failing closed."
            exit 1
          fi
          if [ "${CRITICAL}" -gt 0 ] || [ "${HIGH}" -gt 0 ]; then
            echo ""
            echo "##[error]Trivy found HIGH/CRITICAL vulnerabilities — build blocked before pnpm install."
            echo "##[section]Vulnerability summary"
            echo "  CRITICAL: ${CRITICAL}"
            echo "  HIGH:     ${HIGH}"
            echo ""
            echo "  Severity  CVE                  Package                  Installed  Fixed      Title"
            echo "  --------  -------------------  -----------------------  ---------  ---------  -----"
            jq -r '
              .Results[]? | .Target as $target |
              .Vulnerabilities[]? |
              "  \(.Severity)  \(.VulnerabilityID)  \(.PkgName)  \(.InstalledVersion // "?")  \(.FixedVersion // "no fix")  \(.Title // "")"
            ' trivy-report.json | sort || true
            exit 1
          fi

          echo "##[section]Result"
          echo "✅ Trivy: no HIGH/CRITICAL fixable vulnerabilities found."
        displayName: "Run Trivy once (scan -> JSON, convert to table/SARIF)"

      - task: CopyFiles@2
        displayName: "Collect Trivy reports"
        condition: always()
        inputs:
          contents: |
            trivy-report.json
            trivy-report.sarif
          targetFolder: "$(Build.ArtifactStagingDirectory)/trivy-scan"

      - task: PublishBuildArtifacts@1
        displayName: "Publish Trivy reports"
        condition: always()
        inputs:
          PathtoPublish: "$(Build.ArtifactStagingDirectory)/trivy-scan"
          ArtifactName: "trivy-scan-reports"
          publishLocation: "Container"

  - job: set_image_tag
    displayName: Set Image Tag
    steps:
      - script: |
          if [[ "$(Build.SourceBranch)" =~ refs/heads/(.+) ]]; then
            # Direct branch build
            postfix=""
          elif [[ "$(Build.SourceBranch)" =~ refs/pull/[0-9]+/merge ]]; then
            postfix="_${SYSTEM_PULLREQUEST_SOURCEBRANCH#refs/heads/}"
          elif [[ "$(Build.SourceBranch)" =~ refs/tags/(.+) ]]; then
            postfix="_${BASH_REMATCH[1]}"
          else
            postfix="_unknown"
          fi
          imageTag="$(Build.BuildNumber)$postfix"
          imageTag=${imageTag//\//-}
          imageTag=${imageTag//#/}
          echo "##vso[task.setvariable variable=imageTag]$imageTag"
          echo "$imageTag" > branchInfo.txt
        displayName: 'Set Image Tag and Save to File'

      - task: CopyFiles@2
        displayName: 'Copy branchInfo.txt to Artifacts Dir'
        inputs:
          contents: 'branchInfo.txt'
          targetFolder: '$(Build.ArtifactStagingDirectory)'

      - publish: $(Build.ArtifactStagingDirectory)
        displayName: Publish branchInfo.txt
        artifact: branchInfo

  - job: snomio_build_test_publish
    dependsOn:
      - set_image_tag
      - secrets_scan
      - trivy_scan
    displayName: Build, run integration test and publish
    timeoutInMinutes: 60
    pool:
      name: 'highmemory-pool'
    steps:

      # Download the branchInfo artifact
      - download: current
        artifact: branchInfo
        displayName: "Download branchInfo artifact"

      - script: |
          imageTag=$(cat $(Pipeline.Workspace)/branchInfo/branchInfo.txt)
          if [[ -z "$imageTag" || "$imageTag" == *unknown* ]]; then
            echo "Invalid imageTag: $imageTag"
            exit 1
          fi
          echo "##vso[task.setvariable variable=buildInfo]$imageTag"
          if [ "$(Build.SourceBranch)" = "refs/heads/main" ]; then
            imageTag="$imageTag,latest"
          fi
          echo "##vso[task.setvariable variable=imageTag]$imageTag"
          echo "Using imageTag: $imageTag"
        displayName: 'Read imageTag from branchInfo.txt'

      - task: MavenAuthenticate@0
        inputs:
          artifactsFeeds: '$(feedName)'

      - task: Docker@2
        displayName: 'Login to ACR'
        inputs:
          command: login
          containerRegistry: $(containerRegistry)

      - task: Docker@2
        displayName: 'Login to Quay.io'
        inputs:
          command: login
          containerRegistry: $(quayRegistry)

      - task: Cache@2
        displayName: Cache Maven local repo
        inputs:
          key: 'maven | "$(Agent.OS)" | **/pom.xml'
          restoreKeys: |
            maven | "$(Agent.OS)"
            maven
          path: $(mavenCache)

      - script: mkdir -p $(Pipeline.Workspace)/.pnpm-store
        displayName: 'Create pnpm store directory'

      - task: Cache@2
        displayName: Cache pnpm store
        inputs:
          key: 'pnpm | "$(Agent.OS)" | $(Build.SourcesDirectory)/ui/package.json'
          restoreKeys: |
            pnpm | "$(Agent.OS)"
          path: $(Pipeline.Workspace)/.pnpm-store
          cacheHitVar: CACHE_RESTORED

      # Cache Jib's base-image layer cache so the multi-arch eclipse-temurin:17
      # base isn't re-pulled every build. Requires -Djib.baseImageCache below.
      - script: mkdir -p $(Pipeline.Workspace)/.jib-cache
        displayName: 'Create Jib base-image cache directory'

      - task: Cache@2
        displayName: Cache Jib base-image layers
        inputs:
          key: 'jib | "$(Agent.OS)" | $(Build.SourcesDirectory)/api/pom.xml'
          restoreKeys: |
            jib | "$(Agent.OS)"
          path: $(Pipeline.Workspace)/.jib-cache

      # Cache the frontend-maven-plugin Node/pnpm install dir so node v22 isn't
      # re-extracted every build (keyed on ui/pom.xml which pins the versions).
      - task: Cache@2
        displayName: Cache UI node install dir
        inputs:
          key: 'uinode | "$(Agent.OS)" | $(Build.SourcesDirectory)/ui/pom.xml'
          restoreKeys: |
            uinode | "$(Agent.OS)"
          path: $(Build.SourcesDirectory)/ui/node

      # Cache the resolved node_modules so a frozen install only re-links on a
      # lockfile change. The pnpm store cache avoids downloads but not the linking
      # of ~1200 packages; keying on the lockfile makes this exact and safe.
      - task: Cache@2
        displayName: Cache UI node_modules
        inputs:
          key: 'uinodemodules | "$(Agent.OS)" | $(Build.SourcesDirectory)/ui/pnpm-lock.yaml'
          restoreKeys: |
            uinodemodules | "$(Agent.OS)"
          path: $(Build.SourcesDirectory)/ui/node_modules

      - script: |
          echo "*** Create Maven Local Repo at $(mavenCache)"
          mkdir -p "$(mavenCache)"
          echo "##vso[task.setvariable variable=M2_HOME]/opt/maven"
          mkdir -p ~/.config/pnpm
          echo "store-dir=$(Pipeline.Workspace)/.pnpm-store" > ~/.config/pnpm/rc
        displayName: 'Create Maven Local Repo and set up M2_HOME'

      - task: Maven@3
        displayName: 'Build and run integration test'
        condition: not(and(succeeded(), eq(variables.isRelease, true)))
        inputs:
          mavenPomFile: 'pom.xml'
          options: '-Dsnomio.extensions.sergio.enabled=false -Dlingo.internal.users=$(imsUsername) -Ddocker.registry.host=$(dockerRegistry) -Ddocker.repository=$(dockerRepository) -Djib.to.tags=$(imageTag) -Djib.baseImageCache=$(Pipeline.Workspace)/.jib-cache --no-transfer-progress -DargLine="-Dims-username=$(imsUsername) -Dims-password=$(imsPassword) -Dihtsdo.ims.api.url=$(imsApiUrl) -Dihtsdo.ims.api.cookie.name=$(imsCookieName)"'
          mavenOptions: '$(mavenOptions) -Xmx5072m -Dims-username=$(imsUsername) -Dims-password=$(imsPassword) -Dbuildnumber=$(buildInfo)'
          javaHomeOption: 'path'
          jdkUserInputPath: '/usr/lib/jvm/java-17-openjdk-amd64'
          jdkArchitectureOption: 'x64'
          publishJUnitResults: true
          testResultsFiles: '**/surefire-reports/TEST-*.xml'
          goals: 'package'

      - task: Maven@3
        condition: and(succeeded(), eq(variables.isRelease, true))
        displayName: 'Deploy Release artefacts'
        inputs:
          mavenPomFile: 'pom.xml'
          options: '-Dsnomio.extensions.sergio.enabled=false -Dlingo.internal.users=$(imsUsername) -Ddocker.registry.host=$(dockerRegistry) -Ddocker.repository=$(dockerRepository) -Djib.to.tags=$(imageTag) -Djib.baseImageCache=$(Pipeline.Workspace)/.jib-cache --no-transfer-progress -DargLine="-Dims-username=$(imsUsername) -Dims-password=$(imsPassword) -Dihtsdo.ims.api.url=$(imsApiUrl) -Dihtsdo.ims.api.cookie.name=$(imsCookieName)"'
          mavenOptions: '$(mavenOptions) -Xmx5072m -Dims-username=$(imsUsername) -Dims-password=$(imsPassword) -Dbuildnumber=$(buildInfo)'
          javaHomeOption: 'path'
          jdkUserInputPath: '/usr/lib/jvm/java-17-openjdk-amd64'
          jdkArchitectureOption: 'x64'
          publishJUnitResults: true
          testResultsFiles: '**/surefire-reports/TEST-*.xml'
          goals: 'deploy'

      - script: |
          set -euo pipefail
          
          sudo apt-get update
          sudo apt-get install -y skopeo
          
          echo "Release build detected; copying image(s) from ACR to Quay..."
          echo "imageTag: $(imageTag)"
          
          # imageTag may contain multiple tags separated by commas (e.g. "snomio-...,latest")
          IFS=',' read -ra TAGS <<< "$(imageTag)"
          
          for tag in "${TAGS[@]}"; do
            tag="$(echo "$tag" | xargs)" # trim whitespace
            [ -z "$tag" ] && continue
          
            SRC="docker://$(dockerRegistry)/$(dockerRepository):${tag}"
            DEST="docker://quay.io/aehrc/lingo:${tag}"
          
            echo "Copying (multi-arch) ${SRC} -> ${DEST}"
            skopeo copy --all "${SRC}" "${DEST}"
          done
        displayName: 'Copy multi-arch image from ACR to Quay (release only)'
        condition: and(succeeded(), eq(variables.isRelease, true))

      - task: Maven@3
        condition: not(and(succeeded(), eq(variables.isRelease, true)))
        displayName: 'Deploy Build artefacts'
        inputs:
          mavenPomFile: 'pom.xml'
          # common/auth were already compiled+tested in the package step above;
          # skip re-running their tests here (deploy re-runs the full lifecycle).
          options: '-DskipTests --no-transfer-progress'
          mavenOptions: '$(mavenOptions)'
          javaHomeOption: 'path'
          jdkUserInputPath: '/usr/lib/jvm/java-17-openjdk-amd64'
          jdkArchitectureOption: 'x64'
          publishJUnitResults: false
          goals: 'deploy -pl .,common,auth'

      - task: PublishCodeCoverageResults@1
        condition: not(and(succeeded(), eq(variables.isRelease, true)))
        displayName: Publish Code Coverage
        inputs:
          enabled: true
          codeCoverageTool: 'JaCoCo'
          summaryFileLocation: '**/target/site/jacoco/jacoco.xml'
          reportDirectory: '**/target/site/jacoco'
          additionalCodeCoverageFiles: '**/target/jacoco.exec'


      - script: |
          set -e
          sudo apt-get install -y jq
          if ! command -v mvn &> /dev/null; then
          if [ -n "$M2_HOME" ]; then
            export PATH="$M2_HOME/bin:$PATH"
            echo "Added Maven from M2_HOME to PATH: $M2_HOME/bin"
          else
            echo "Maven not found and M2_HOME is not properly set. Please install Maven or set M2_HOME correctly."
            exit 1
            fi
          fi
          if ! command -v mvn &> /dev/null; then
            echo "Maven command still not found after adding M2_HOME to PATH. Check that the Maven binary exists at $M2_HOME/bin/mvn"
            exit 1
          fi
          ORGANIZATION=$(echo $SYSTEM_COLLECTIONURI | sed 's|https://dev.azure.com/\(.*\)/|\1|')
          echo "Organization: $ORGANIZATION"
          PROJECT=$SYSTEM_TEAMPROJECT
          echo "Project: $PROJECT"
          FEED=$FEED_NAME
          echo "Feed: $FEED"
          API_VERSION="7.1-preview.1"
          BASE_URL="https://pkgs.dev.azure.com/${ORGANIZATION}/${PROJECT}/_apis/packaging/feeds/${FEED}/maven/groups"
          if [ -z "$SYSTEM_ACCESSTOKEN" ]; then
            echo "SYSTEM_ACCESSTOKEN is not set. Enable 'Allow scripts to access the OAuth token' in the pipeline settings."
            exit 1
          fi
          promote_artifact() {
            local group_id=$1
            local artifact_id=$2
            local version=$3

            echo "Promoting ${group_id}:${artifact_id} version ${version}"

            # Promote the version to the Release view
            curl -s -X PATCH "${BASE_URL}/${group_id}/artifacts/${artifact_id}/versions/${version}?api-version=${API_VERSION}" \
              -H "Authorization: Bearer ${SYSTEM_ACCESSTOKEN}" \
              -H "Content-Type: application/json" \
              -d '{
                  "views":  {
                              "path":  "/views/-",
                              "op":  "add",
                              "value":  "Release"
                            }
                  }'

            echo "Promotion completed for ${group_id}:${artifact_id}:${version} "
          }
          POM_FILES=$(find . -name "pom.xml" -not -path "./target/*" -not -path "./*/target/*")
          for pom in $POM_FILES; do
            echo "Processing POM: $pom"
            GROUP_ID=$(mvn help:evaluate -Dexpression=project.groupId -q -DforceStdout -f "$pom")
            ARTIFACT_ID=$(mvn help:evaluate -Dexpression=project.artifactId -q -DforceStdout -f "$pom")
            VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout -f "$pom")
            promote_artifact "$GROUP_ID" "$ARTIFACT_ID" "$VERSION"
          done
        displayName: 'Promote artifacts to Release view'
        condition: and(succeeded(), eq(variables.isRelease, true))
        env:
          SYSTEM_ACCESSTOKEN: $(System.AccessToken)
          SYSTEM_COLLECTIONURI: $(System.CollectionUri)
          SYSTEM_TEAMPROJECT: $(System.TeamProject)
          FEED_NAME: $(feedName)
